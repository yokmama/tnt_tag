# Commands（コマンド一覧）

TNT TAGプラグインで使用できる全コマンドと権限ノードの説明です。

## 目次

- [プレイヤーコマンド](#プレイヤーコマンド)
- [管理者コマンド](#管理者コマンド)
- [権限ノード](#権限ノード)
- [コマンドエイリアス](#コマンドエイリアス)

## プレイヤーコマンド

### `/tnttag join [arena]`

ゲームに参加します。

**使用方法**:
```
/tnttag join           # デフォルトアリーナに参加
/tnttag join arena1    # 指定したアリーナに参加
```

**権限**: `tnttag.play`（デフォルト: true）

**説明**:
- アリーナ名を省略した場合、利用可能なアリーナに自動参加
- 既にゲーム中の場合はエラーメッセージが表示されます
- 最大プレイヤー数に達している場合は参加できません

**出力例**:
```
✓ ゲームに参加しました！
✓ 現在の参加者: 15/25人
✓ ゲーム開始まで あと5人
```

---

### `/tnttag leave`

現在参加中のゲームから退出します。

**使用方法**:
```
/tnttag leave
```

**権限**: `tnttag.play`（デフォルト: true）

**説明**:
- ゲーム中でも退出可能
- 退出すると元の位置に戻ります
- ゲーム進行中の退出は統計に記録されます

**出力例**:
```
✓ ゲームから退出しました
```

---

### `/tnttag stats [player]`

プレイヤーの統計情報を表示します。

**使用方法**:
```
/tnttag stats          # 自分の統計を表示
/tnttag stats Player1  # 他のプレイヤーの統計を表示
```

**権限**:
- `tnttag.stats`（自分の統計、デフォルト: true）
- `tnttag.stats.others`（他人の統計、デフォルト: op）

**説明**:
- プレイ回数、勝利数、生存ラウンド数などを表示
- 他のプレイヤーの統計は権限が必要

**出力例**:
```
========== Player1の統計 ==========
総プレイ回数: 50回
勝利回数: 8回
勝率: 16.0%

平均生存ラウンド: 4.2
最高記録: 6ラウンド生存

TNT保持回数: 120回
タグ成功回数: 85回
爆発回数: 42回
================================
```

---

### `/tnttag list`

利用可能なアリーナの一覧を表示します。

**使用方法**:
```
/tnttag list
```

**権限**: `tnttag.play`（デフォルト: true）

**説明**:
- 全アリーナの状態を表示
- 参加可能なアリーナを確認できます

**出力例**:
```
========== アリーナ一覧 ==========
[🟢 参加可能] arena1 (15/25人)
[🔴 ゲーム中] arena2 (22/25人)
[⚪ 準備中] arena3 (0/25人)
================================
```

---

## 管理者コマンド

管理者コマンドは全てOP権限または`tnttag.admin`権限が必要です。

### `/tnttag setpos1`

アリーナの角1（対角線の一点）を設定します。

**使用方法**:
```
/tnttag setpos1
```

**権限**: `tnttag.admin`

**説明**:
- 現在立っている位置を角1として記録
- `setpos2`と組み合わせてアリーナの範囲を定義

**出力例**:
```
✓ 位置1を設定しました: X=100, Y=64, Z=200
```

---

### `/tnttag setpos2`

アリーナの角2（対角線のもう一点）を設定します。

**使用方法**:
```
/tnttag setpos2
```

**権限**: `tnttag.admin`

**説明**:
- 現在立っている位置を角2として記録
- `setpos1`と組み合わせてアリーナの範囲を定義

**出力例**:
```
✓ 位置2を設定しました: X=150, Y=80, Z=250
✓ 範囲: 50x16x50ブロック
```

---

### `/tnttag creategame <arena>`

新しいアリーナを作成します。

**使用方法**:
```
/tnttag creategame arena1
```

**権限**: `tnttag.admin`

**説明**:
- `setpos1`と`setpos2`で設定した範囲でアリーナを作成
- スポーン地点は範囲の中心に自動設定

**前提条件**:
- `setpos1`と`setpos2`が設定済みであること
- 同じ名前のアリーナが存在しないこと

**出力例**:
```
✓ アリーナ 'arena1' を作成しました
✓ スポーン地点: X=125, Y=64, Z=225
✓ /tnttag start arena1 でゲームを開始できます
```

---

### `/tnttag delete <arena>`

アリーナを削除します。

**使用方法**:
```
/tnttag delete arena1
```

**権限**: `tnttag.admin`

**説明**:
- 指定したアリーナを完全に削除
- ゲーム中のアリーナは削除できません

**出力例**:
```
✓ アリーナ 'arena1' を削除しました
```

---

### `/tnttag start <arena>`

ゲームを強制的に開始します。

**使用方法**:
```
/tnttag start arena1
```

**権限**: `tnttag.admin`

**説明**:
- 最小プレイヤー数に満たなくてもゲームを開始
- テストや特別なイベントで使用

**出力例**:
```
✓ ゲームを強制開始しました
✓ 参加者: 10人（最小: 20人）
```

---

### `/tnttag stop <arena>`

進行中のゲームを強制終了します。

**使用方法**:
```
/tnttag stop arena1
```

**権限**: `tnttag.admin`

**説明**:
- ゲームを即座に終了
- 全プレイヤーがロビーに戻ります
- 統計には記録されません

**出力例**:
```
✓ ゲームを強制終了しました
✓ 全プレイヤーをロビーに戻しました
```

---

### `/tnttag reload`

設定ファイルを再読み込みします。

**使用方法**:
```
/tnttag reload
```

**権限**: `tnttag.admin`

**説明**:
- config.yml、arenas.yml、messagesを再読み込み
- サーバーを再起動せずに設定を反映
- ゲーム中のアリーナには影響しません

**出力例**:
```
✓ 設定ファイルを再読み込みしました
✓ config.yml: OK
✓ arenas.yml: OK
✓ messages_ja_JP.yml: OK
```

---

## 権限ノード

### プレイヤー権限

| 権限ノード | 説明 | デフォルト |
|----------|------|----------|
| `tnttag.play` | ゲームへの参加 | true |
| `tnttag.stats` | 自分の統計閲覧 | true |
| `tnttag.stats.others` | 他人の統計閲覧 | op |
| `tnttag.spectate` | 観戦モード | true |

### 管理者権限

| 権限ノード | 説明 | デフォルト |
|----------|------|----------|
| `tnttag.admin` | 全ての管理者コマンド | op |
| `tnttag.admin.create` | アリーナ作成 | op |
| `tnttag.admin.delete` | アリーナ削除 | op |
| `tnttag.admin.start` | ゲーム強制開始 | op |
| `tnttag.admin.stop` | ゲーム強制終了 | op |
| `tnttag.admin.reload` | 設定再読み込み | op |

### 特別な権限

| 権限ノード | 説明 | デフォルト |
|----------|------|----------|
| `tnttag.vip` | VIP機能（優先参加） | false |
| `tnttag.bypass.cooldown` | クールダウン無視 | op |

## 権限の設定例

### LuckPermsを使用する場合

```bash
# プレイヤーに基本権限を付与
lp user Player1 permission set tnttag.play true

# VIP権限を付与
lp user Player1 permission set tnttag.vip true

# 管理者権限を付与
lp user Admin1 permission set tnttag.admin true

# グループに権限を付与
lp group default permission set tnttag.play true
lp group vip permission set tnttag.vip true
lp group admin permission set tnttag.admin true
```

### permissions.yml を使用する場合

```yaml
groups:
  default:
    permissions:
      tnttag.play: true
      tnttag.stats: true
      tnttag.spectate: true

  vip:
    inheritance:
      - default
    permissions:
      tnttag.vip: true
      tnttag.stats.others: true

  admin:
    inheritance:
      - vip
    permissions:
      tnttag.admin: true
```

## コマンドエイリアス

以下の短縮形も使用できます：

| 元のコマンド | エイリアス |
|----------|----------|
| `/tnttag` | `/tnt` |
| `/tnttag join` | `/tnt j` |
| `/tnttag leave` | `/tnt l` |
| `/tnttag stats` | `/tnt s` |
| `/tnttag list` | `/tnt ls` |

**使用例**:
```
/tnt j arena1      # /tnttag join arena1 と同じ
/tnt l             # /tnttag leave と同じ
/tnt s Player1     # /tnttag stats Player1 と同じ
```

## タブ補完

全てのコマンドはタブ補完に対応しています：

```
/tnttag [TAB]      # サブコマンドの候補を表示
/tnttag join [TAB] # アリーナ名の候補を表示
/tnttag stats [TAB] # オンラインプレイヤーの候補を表示
```

---

**関連ページ**:
- [Configuration](Configuration) - 設定ファイルの詳細
- [Arena Setup](Arena-Setup) - アリーナの作成方法
- [Troubleshooting](Troubleshooting) - よくある問題
